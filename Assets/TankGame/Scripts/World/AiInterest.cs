using UnityEngine;

namespace TankGame {
    public class AiInterest : MonoBehaviour {
        // Stub. We have potential to expand this if we need to add
        // anything to make the AI smarter 
    }
}